# Gate config validation - fm/gate-configs-vmlease

## 1. Change scope: exactly 3 files, config-only (no source/behavior changes)
 .gitignore        |  2 ++
 .no-mistakes.yaml | 31 +++++++++++++++++++++++++++++++
 treehouse.toml    |  7 +++++++
 3 files changed, 40 insertions(+)

Files touched under src/ or tests/ (expect none):
  (none)

## 2. .gitignore delta (adds only .nm-home/)
+# Containerized-gate HOME (uv caches/toolchains inside no-mistakes runs)
+.nm-home/

## 3. .no-mistakes.yaml parses; commands map to real Makefile targets
  top-level keys: ['commands', 'test']
  commands: ['lint', 'test']
  lint  -> True
  test  -> make typecheck test coverage: True
  test.evidence.store_in_repo: True

  Makefile targets present:
    lint:  ## strict lint, no autofix
    lint-battery:  ## shellcheck the example battery bundle (severity-gated gate; requires shellcheck)
    typecheck:  ## strict type check
    test:  ## run the suite (warnings are errors; per-test output shown only on failure)
    coverage:  ## run the suite under coverage + report (fails below the floor)

## 4. treehouse.toml parses; max_trees == 4
  parsed: {'max_trees': 4, 'root': ''}
  max_trees == 4: True
