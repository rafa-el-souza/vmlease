max_trees = 4
root = ""

# Worktree root directory (relative to repo root or absolute path).
# Worktrees are placed under {root}/.treehouse/. Default: $HOME
# max_trees = 4 is the fleet-wide concurrency ceiling (workflow design:
# firstmate has no dispatch cap of its own; the pool is the structural limit).
